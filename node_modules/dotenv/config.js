(function () {
  var options = {}
  // console.log(process.env);
  process.argv.forEach(function (val, idx, arr) {

    var matches = val.match(/^dotenv_config_(.+)=(.+)/) //regex
    if (matches) {
      options[matches[1]] = matches[2];
    }
  })

  require('./lib/main').config(options)
})()
